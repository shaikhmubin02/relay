"""Relay - a volunteer shift-recovery agent.

Relay turns a volunteer cancellation into confirmed coverage. The language model
interprets and drafts; deterministic code enforces every rule and performs every
side effect.
"""

__version__ = "0.1.0"
